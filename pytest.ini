[pytest]
testpaths = tests
# tools/ is a flat folder of scripts (they `import config`, `import _common`),
# not a package - put it on sys.path so tests can `import relocate_offset`.
pythonpath = tools
